import 'package:flutter/material.dart';

class AllComponentScreen extends StatelessWidget {
  const AllComponentScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
