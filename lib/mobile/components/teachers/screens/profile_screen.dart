import 'package:flutter/material.dart';

class ProfileScreenTeachers extends StatelessWidget {
  const ProfileScreenTeachers({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
